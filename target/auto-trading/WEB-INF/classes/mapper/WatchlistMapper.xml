<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.autotrading.mapper.WatchlistMapper">

    <select id="findAll" resultType="com.autotrading.model.WatchlistItem">
        SELECT id, symbol, exchange, created_at
        FROM tb_auto_watchlist
        ORDER BY id DESC
    </select>

    <select id="findBySymbol" resultType="com.autotrading.model.WatchlistItem">
        SELECT id, symbol, exchange, created_at
        FROM tb_auto_watchlist
        WHERE symbol = #{symbol}
        ORDER BY id DESC
        LIMIT 1
    </select>

    <insert id="insert">
        INSERT INTO tb_auto_watchlist(symbol, exchange, created_at)
        VALUES(#{symbol}, #{exchange}, CURRENT_TIMESTAMP)
    </insert>

    <delete id="delete">
        DELETE FROM tb_auto_watchlist WHERE id = #{id}
    </delete>
</mapper>
